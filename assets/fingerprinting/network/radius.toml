[service]
name = "radius"
default_ports = [1812, 1813, 1645, 1646]
description = "RADIUS authentication and accounting"

# No probe yet: an Access-Request is authenticated with a shared secret, and the
# Access-Reject that comes back needs a decoder before it can be read.
