[service]
name = "openvpn"
default_ports = [1194]
description = "OpenVPN"

# No probe yet. A hard-reset draws a reply from 1194 itself, so this one could be
# authored; a server using tls-auth or tls-crypt discards it unsigned.
