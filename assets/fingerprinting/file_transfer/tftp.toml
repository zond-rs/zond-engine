[service]
name = "tftp"
default_ports = [69]
description = "Trivial File Transfer Protocol"

# No probe, and not for want of a decoder. RFC 1350 has the server answer from a
# fresh transfer identifier, so the error arrives from an ephemeral port and 69
# only ever receives. See the correlation note in scanner/strategy/ports/udp.rs.
